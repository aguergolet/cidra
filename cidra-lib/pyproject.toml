[tool.poetry]
name = "cidra-lib"
version = "0.1.0"
description = "A library for CIDRA functionality."
authors = ["Andre <aguergolet@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[project.urls]
Homepage = "https://github.com/aguergolet/cidra-server"
Issues = "https://github.com/aguergolet/cidra-server/issues"